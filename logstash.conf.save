input {
  beats {
    host => "0.0.0.0"
    port => 5044
  }
}

filter {
 grok {
   match => [""]
   overwrite => [ "message" ]
 }

output {
  elasticsearch {
    hosts => ["192.168.1.72:9200"]
    manage_template => false
    index => "%{[@metadata][beat]}-%{[@metadata][version]}-%{+YYYY.MM.dd}"
  }
}
